<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>HexFlora · eHerbarium</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=New+Tegomin&display=swap" rel="stylesheet">
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <div class="container">
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
        <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
        <span class="fs-1" style="font-family: 'New Tegomin', serif;">HexFlora · eHerbarium</span>
      </a>

      <ul class="nav nav-pills">
        <li class="nav-item"><a href="#" class="nav-link">Herbarium</a></li>
        <li class="nav-item"><a href="<%= Routes.page_path(@conn, :flora) %>" class="nav-link <%= if @conn.private.phoenix_action == :flora do %>active<% end %>">Flora of the Hex</a></li>
        <li class="nav-item">
          <a href="<%= Routes.page_path(@conn, :about) %>" class="nav-link <%= if @conn.private.phoenix_action == :about do %>active<% end %>">About</a>
        </li>
        <li class="nav-item">
          <a href="<%= Routes.page_path(@conn, :contribute) %>" class="nav-link <%= if @conn.private.phoenix_action == :contribute do %>active<% end %>">Contribute</a>
        </li>
      </ul>
    </header>
    </div>
    <main role="main" class="container">
      <%= @inner_content %>
    </main>
  </body>
</html>
